package com.example.divyanshukumar.yourapp;

import android.content.Context;
import android.content.Intent;
import android.content.pm.PackageInfo;
import android.view.View;
import android.widget.AdapterView;
import android.widget.ListView;

public class PKname {

private String pname;

//    public void getPKname( final ListView list,final Context context){

//       final Testing badaname = new Testing();
//        pname = badaname.nameOf.get(0);


//        list.setOnItemClickListener(new AdapterView.OnItemClickListener() {
//            @Override
//            public void onItemClick(AdapterView<?> parent, View view, int position, long id) {
//
//                String apps = (String) list.getAdapter().getItem(position);
//
////                Intent intent = new Intent(Intent.ACTION_SEND);
////                intent.setType("text/html");
////                intent.putExtra(Intent.EXTRA_EMAIL, "emailaddress@emailaddress.com");
////                intent.putExtra(Intent.EXTRA_SUBJECT, "Subject");
////                intent.putExtra(Intent.EXTRA_TEXT, "I'm email body.");
//
////                list.getContext().startActivity(Intent.createChooser(intent, "Send Email"));
//
//
//
//
////                Intent intent = context.getPackageManager().getLaunchIntentForPackage("com.truecaller");
//                Intent intent = context.getPackageManager().getLaunchIntentForPackage(nameOf.get(0));
//
//
//                list.getContext().startActivity(intent);
//
//            }
//        });

    }



